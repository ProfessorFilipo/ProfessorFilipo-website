# /database

- schema.sql — creates all tables in Postgres (Neon)

To apply: use Neon's own SQL Editor (in the Neon console), or connect via a
Postgres client (psql, PyCharm Professional's Database tool, etc.) using the
project's connection string.
